<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Football Play Tracker</title>
<style>
body { font-family: Arial; padding: 10px; background: #f0f0f0; }
.player { background: white; border-radius: 8px; margin: 5px 0; padding: 10px; display: flex; justify-content: space-between; align-items: center; box-shadow: 0 0 5px #aaa; }
.in { background: #d4fcd4; }
.done { background: #ffdddd; }
button { margin-left: 5px; }
</style>
</head>
<body>

<h2>📋 Football Play Tracker</h2>
<div id="players"></div>

<script>
const requiredPlays = 4;
const playerCount = 30;
let players = [];

// Initialize players
for (let i = 1; i <= playerCount; i++) {
players.push({ name: `Player ${i}`, plays: 0, inGame: false });
}

function renderPlayers() {
const container = document.getElementById('players');
container.innerHTML = '';

players.forEach((p, index) => {
const div = document.createElement('div');
div.className = 'player';
if (p.inGame) div.classList.add('in');
if (p.plays >= requiredPlays) div.classList.add('done');

div.innerHTML = `
<strong>${p.name}</strong>
<span>Plays: ${p.plays}</span>
<div>
<button onclick="toggleInGame(${index})">${p.inGame ? 'Sub Out' : 'Sub In'}</button>
<button onclick="addPlay(${index})">+1 Play</button>
</div>
`;

container.appendChild(div);
});
}

function toggleInGame(index) {
players[index].inGame = !players[index].inGame;
renderPlayers();
}

function addPlay(index) {
if (players[index].inGame) {
players[index].plays++;
renderPlayers();
}
}

renderPlayers();
</script>

</body>
</html>